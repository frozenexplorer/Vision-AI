{
  "name": "visionai",
  "version": "1.2.0",
  "private": true,
  "description": "VisionAI monorepo: frontend (React Native CLI), backend, models",
  "scripts": {
    "start": "npm run start --prefix frontend",
    "android": "npm run android --prefix frontend",
    "android:install-dev": "npm run android:install-dev --prefix frontend",
    "android:apk": "npm run android:apk --prefix frontend",
    "ios": "npm run ios --prefix frontend",
    "frontend": "npm run start --prefix frontend"
  }
}
